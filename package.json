{
  "name": "cobertec-intake",
  "version": "0.1.0",
  "private": true,
  "description": "Sistema de intake inteligente para soporte técnico Cobertec — Fase 1 MVP",
  "scripts": {
    "dev:backend": "cd backend && npm run dev",
    "dev:frontend": "cd frontend && npm run dev",
    "test": "cd backend && npm test",
    "test:classifier": "cd backend && npm run test:classifier"
  },
  "dependencies": {
    "@fastify/cookie": "^11.0.2",
    "bcrypt": "^6.0.0",
    "fastify-plugin": "^5.1.0",
    "jsonwebtoken": "^9.0.3"
  },
  "devDependencies": {
    "@types/bcrypt": "^6.0.0",
    "@types/jsonwebtoken": "^9.0.10"
  }
}
